import mysql.connector

db = mysql.connector.connect(
  host="localhost",
  user="root",
  password="root",
  database="Family"
)

mycursor = db.cursor()

mycursor.execute("create table Members(Name VARCHAR(255), Age INT)")
